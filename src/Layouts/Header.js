import React from 'react'
import {AppBar, Toolbar} from '@material-ui/core/';

export default function Header() {
    return (
        <React.Fragment>
            <AppBar>
                <Toolbar>
                    
                </Toolbar>
            </AppBar>
        </React.Fragment>
    )
}
